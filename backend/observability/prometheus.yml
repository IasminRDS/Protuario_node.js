global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: snpe-api
    metrics_path: /metrics
    static_configs:
      - targets: ['api:3000']
        labels:
          service: snpe-api
